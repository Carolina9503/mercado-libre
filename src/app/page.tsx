import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Search Page",
  description: "Prueba mercado libre"
};

const page = () => {
  return null;
};

export default page;
